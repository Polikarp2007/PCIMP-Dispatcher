<UserControl x:Class="PCMPDispatcher.MapView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf">
    <Grid Background="White">
        <wv2:WebView2 x:Name="WebMap"/>
        <TextBlock x:Name="ErrorText" Visibility="Collapsed"
                   Text="Install Microsoft Edge WebView2 Runtime to enable the map"
                   FontSize="13" FontWeight="SemiBold" Foreground="#555555"
                   HorizontalAlignment="Center" VerticalAlignment="Center"
                   FontFamily="Segoe UI"/>
    </Grid>
</UserControl>
